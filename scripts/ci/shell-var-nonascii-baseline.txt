# shell-var-nonascii baseline —— 「已知但本 PR 不得修」的違規清單（其餘一律當新違規擋下）
#
# 格式（TSV，3 欄）:  <path>\t<違規行內的片段>\t<理由>
#   - 片段用「原文子字串」比對 ⇒ 該行一旦改動，本項會自動失效（並印 ⚠️ stale 警告要你刪掉）。
#   - 只放「別的 PR 正在修、動了會衝突」的項。**新違規一律不准加進來**（要修，不要 baseline）。
#   - 每次檢查都會把略過項印出來（⚠️），所以不會靜默被遺忘。
#
# 現況：**空（0 項）**。
#   - 2026-09-26：原本有 1 項（scripts/alertmanager-webhook/install-webhook.sh:96）⇒ PR #2020 併入後
#     已修，stale 警告要求刪除 ⇒ 本檔清空（保留格式說明，供下一個「別的 PR 正在修」的項使用）。
