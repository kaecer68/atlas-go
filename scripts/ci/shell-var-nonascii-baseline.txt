# shell-var-nonascii baseline —— 「已知但本 PR 不得修」的違規（其餘一律當新違規擋下）
#
# 格式（TSV，3 欄）:  <path>	<違規行內的片段>	<理由>
#   - 片段用「原文子字串」比對 ⇒ 該行一旦改動，本項會自動失效（並印 ⚠️ stale 警告要你刪掉）。
#   - 只放「別的 PR 正在修、動了會衝突」的項。**新違規一律不准加進來**（要修，不要 baseline）。
#   - 每次檢查都會把略過項印出來（⚠️），所以不會靜默被遺忘。
#
# 現況：僅 1 項，來自 scripts/alertmanager-webhook/install-webhook.sh:96；同型 bug 已在
#       PR #2020（branch fix/20260926-negative-proof-exact-rc）修掉 ⇒ 該 PR 併入後請刪除本行。
scripts/alertmanager-webhook/install-webhook.sh	placeholder（$PLACEHOLDER）	PR #2020（#2011）正在修同一行；本 PR 不動該檔以免衝突（併入後刪除本行）
