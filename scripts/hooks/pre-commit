#!/usr/bin/env bash
# ⛔ DEPRECATED — This file is replaced by .githooks/pre-commit
#
# Install the active version:
#   bash scripts/install-hooks.sh
#
# Why deprecated:
#   - Phase 1 (auto-stage generated files) CONFLICTS with .githooks Check 5
#     (drift blocker). Auto-staging silently commits unintended changes.
#   - Phase 2 (binary detection) is a subset of .githooks Check 1.
#   - Phase 1.5 (map generation) is NOT yet ported — see TODO below.
#
# TODO: Port map generation (cmd/mapgen -gen-all) to .githooks/pre-commit as Check 6.

echo "⛔ scripts/hooks/pre-commit is DEPRECATED."
echo "   Install the active pre-commit hook: bash scripts/install-hooks.sh"
echo "   See .githooks/pre-commit for the current 5-check implementation."
exit 0
