# atlas-modernize-20260829 — k3 Code Review

**Reviewer**: kimi-coding/k3 (code-reviewer role)
**Date**: 2026-08-29
**Base**: 6ca24995 (merge-base vs main) → HEAD 7d6ec66c (4 commits, 272 files, +1074/-1071)

## Verdict: ✅ APPROVE

四個 commit 皆為高度機械性的 modernize 轉換。我獨立重跑驗證並逐 hunk 比對，未發現語意不等價、lint 回歸、漏改/誤刪、build-tag 遺漏四類問題。兩個 comment 級觀察（見 §4），不阻擋合併。

---

## 1. 獨立驗證結果（重跑，非引用報告）

| 檢查 | 命令 | 結果 |
|---|---|---|
| go vet | `go vet ./...` | ✅ clean (exit 0) |
| golangci-lint | `golangci-lint run --timeout=5m ./...` | ✅ 0 issues |
| 測試 | `go test ./...` | ✅ 165 ok / 0 FAIL |
| integration build tag | `go vet -tags integration ./internal/ledger/` | ✅ clean |
| 殘留參考 | grep 已刪 helper 名 (boolPtr/intPtr/float64Ptr/ptrFloat64/ptrInt64/periodPtr) | ✅ production 無殘留；test 檔殘留者皆為「保留改寫版」且仍在使用 |

## 2. Standards 軸

| 項目 | 結論 | 依據 |
|---|---|---|
| .golangci.yml 相容 | ✅ | gosec G602 未排除 → 11 處保留 3-clause 迴圈是正確決策（轉了會 lint 回歸）。實測 lint 0 issues |
| gofumpt/gci | ✅ | 全 tree gofumpt -l 僅 5 個 **pre-existing** 髒檔（base 6ca24995 已髒、本分支未觸及），非回歸 |
| go-core.instructions.md | ✅ | 無公開 API 簽名變更（risk API golden 僅型別字串 `interface{}`→`any`）；改動全在 internal//cmd/（applyTo 範圍）；import 分組未動 |
| go 版本 | ✅ | go.mod go 1.26.4；range-over-int (1.22+)、any (1.18+)、new(expr) (1.26)、//go:fix inline 皆合法 |
| 未觸及區域 | ✅ | CI workflows / Makefile / go.mod / data/ 零改動；integration-tagged postgres_historical_test.go 未被 batch2 誤改 |

## 3. Spec 軸（語意等價驗證）

### batch1 rangeint (1619f8ba) — ✅ 機械驗證全數通過
- 678 處轉換 = 678 行 for-header 一對一替換，**無任何附帶修改**（全部 678 minus/plus 行皆為 for 行）。
- 逐 hunk multiset 比對（545 hunks）：limit 全部一致（`len(x)`→`range x` 11 處、`K-1`→`K - 1` 僅 gofmt 空白差異 2 處）；loop var 保留時名稱 100% 相符，刪除者（164 處 `for range N`）皆為 body 未使用。
- 無 limit 為函數呼叫的非 len 案例（逐次求值風險不存在）。
- 抽查含 break/continue 的 8 檔（charter/stats、optimizer_math、dcc_garch、backtest 等）：控制流行為不變。

### batch2 any (7763a318) — ✅
- 143 個 `interface{}` 移除 = 143 個 `any` 加入（135 程式碼 + 8 golden JSON 字串），零殘留於預設 build。
- 26 行「無 interface{} 的 minus 行」全為 struct 欄位 **gofmt 對齊重排**（`interface{}`→`any` 變短），逐一核對為純空白。
- 無字串 literal / struct tag / 註解被誤改。
- golden 更新 4 行，golden 與 actual 完全相同，僅 go/types 型別字串表示。✅ 符合「僅字串表示」宣稱。
- 剩 3 處 interface{}：2 註解 + 1 integration-tag 檔，已逐一確認。

### batch3 newexpr (60333a5d) — ✅
- 4 個 production helper 刪除（tools.go boolPtr/intPtr、system/handlers.go float64Ptr、stocktools boolPtr）：全 tree grep 零殘留參考，含 build-tag 檔。
- 14 個 test helper 改寫為 `new(expr)` + `//go:fix inline`，皆仍被本檔使用（unused linter 不報因 _test.go 被排除，但 grep 確認有 call sites）。
- `new(v)` ≡ `&v`（參數複本取址）語意等價；`new(expr)` 型別由表達式推導，與原 helper 回傳型別一致。
- 無 `new` shadowing：tree 內 3 個 `new` 作為變數名的檔案均不在 batch3 改動清單內。

### batch1-followup (7d6ec66c) — ✅
- `for j := 0; j < i; j++` → `for j := range i`：i 為外層 range var，進入內層時求值一次，等價。
- `for i := 0; i < day; i++` → `for range day`：i 未使用，等價。

### 11 處保留（rangeint 剩餘）— ✅ 合理
逐一確認 spillover.go ×6 / bayesian_optimizer.go / randomforest.go 的保留迴圈皆以 loop var 做 slice 索引（`normalised[i][j]`、`vars[i]`、`F[(lag+1)*K+i]`），gosec G602 對 `range K`（K 為變數）無法證明邊界 → 保留可避免 9 個 false positive，符合 repo「lint 0 issues」鐵律。

## 4. Comment 級觀察（非阻擋）

1. **被刪註解含領域知識**：`internal/stocktools/handler.go` 的 boolPtr doc comment（「nil = 未評估，false 是明確訊號，故不能省略 false」）隨 helper 刪除消失。此語義仍適用於 `domain.Quote.Complete/TradingDay`。建議把該句移到 domain.Quote 欄位註解。屬文件性損失，不影響正確性。
2. **11 處保留迴圈無標記**：未來若有人（或 modernize 再跑）想轉換它們，沒有註解說明「刻意保留給 gosec G602」。可考慮加一行 `// 3-clause retained: gosec G602 cannot prove bounds for range-over-int`（或寫進 report 已足夠，view 維護者偏好）。
3. **golangci `run.go: "1.25"` vs go.mod 1.26.4**：lint 分析語言版本低於編譯版本，本次未造成誤報，但 new(expr) 是 1.26 特性，建議日後把 lint go 版本對齊（非本 PR 範圍）。

## 5. 逐 commit 意見

| Commit | Verdict | 備註 |
|---|---|---|
| 1619f8ba batch1 rangeint | approve | 逐 hunk 機械驗證通過；零附帶修改 |
| 7763a318 batch2 any | approve | 純替換+gofmt 重排+golden 字串；integration 檔未誤觸 |
| 60333a5d batch3 newexpr | approve (+comment) | helper 刪除安全；建議保留 boolPtr 領域註解於 domain 欄位 |
| 7d6ec66c batch1-followup | approve | 2 處皆等價 |

## 6. 總結

**Approve。** 四類目標問題（語意不等價 / lint 回歸 / 漏改誤刪 / build-tag 遺漏）逐一排查皆陰性。批次切分合理（每批可獨立 revert），驗證紀律良好（每批 vet+lint+test）。剩餘 350 診斷的後續批次建議（minmax/stringscutprefix/mapsloop/slicescontains 先行，omitzero/atomictypes 人工確認）方向正確。
