# Stress AGENTS.md

## 模組概述

`stress` 提供壓力測試場景執行 — `RunScenario()`。

// Maturity: experimental

## 關鍵符號

- `RunScenario()` — 執行壓力測試場景

## 已知陷阱

- **實驗性質**：API 不穩定，不應被 stable/evolving 模組依賴。
- **情境模擬**：結果為模擬數據，不反映真實市場行為。

## 相依關係

- 獨立測試模組
