# PR #554 — Orchestrator Coverage Push

**Verdict:** ✅ APPROVED WITH INFORMATIONAL NOTES
**Merged:** 2026-06-15T15:54:44Z (commit `fe263546`)
**Author HEAD (pre-merge):** `135cfcd4` (rebased from `29288708`)
**Base:** `main` @ `9c3dc70a`

---

## Stats

| Field | Value |
|-------|-------|
| Files changed | 3 |
| Insertions | 833 |
| Deletions | 0 |
| Production code changes | 0 |
| `go test ./internal/orchestrator/...` | PASS |
| `staticcheck ./internal/orchestrator/...` | clean |
| Coverage (statements) | 59.9% |

### Coverage gain — `internal/orchestrator/plugin_adapters.go`

| Method | Before | After |
|--------|--------|-------|
| `Name` | 0% | 100.0% |
| `Attach` | 0% | 100.0% |
| `ProcessRecommendations` | 0% | 100.0% |
| `PostSimulation` | 0% | 93.3% |

---

## Findings (9 — all INFORMATIONAL, 0 blocking)

| # | Severity | Category | Summary |
|---|----------|----------|---------|
| 1 | ~~CRITICAL~~ → resolved | scope | Branch initially included `system.go` / `system_record_summary_retry.go` changes — **FIXED by `git rebase --autostash origin/main` before merge** |
| 2 | INFO | coverage | `maxAttempts<1` default-to-1 branch in `RecordSummaryWithRetry` not covered |
| 3 | INFO | coverage | `ResolvePrompt` only tests the override-map path; resolver injection and `os.ReadFile` fallback untested |
| 4 | INFO | coverage | `loadRecOverrides` edge cases (expired interventions, empty `TargetSymbol`) not exercised |
| 5 | INFO | brittleness | Multiple tests assert private fields: `sys.Risk().eventBus`, `sys.drawdownReporter`, `reg.promptResolver`, `sc.provider` |
| 6 | INFO | naming | `_Cov` suffix on test names; `_coverage_test.go` filename non-standard for this codebase |
| 7 | INFO | redundancy | `plugin_adapters_coverage_test.go` largely duplicates existing `plugin_adapters_test.go`; several functions are low-value smoke tests |
| 8 | INFO | coverage | `CalculateFactorScores` tests only assert non-nil; no numeric value or component checks |
| 9 | INFO | smoke | `TestPluginRegistry_WithSetters` is a pure smoke test (no meaningful assertions) |

### Pass (no issues found)

- ✅ Mock correctness (`mockPositionEvaluator`, `mockFactorQueryCoverage`, `mockServiceRegistry`, `screeningMetricsCollector`, `humanInterventionStore` correctly implement their interfaces)
- ✅ Test independence (no shared mutable state)
- ✅ Resource cleanup (`t.TempDir` usage, no leaked resources)
- ✅ Lint / staticcheck

---

## Pre-existing issues (NOT from this PR, documented for context)

These exist on `test/coverage-70-push @ 5c76315a` and were inherited by the branch:

- `internal/monitoring` build failure (redeclared tests)
- `internal/risk/macro_aware_drawdown_test.go` gofmt violation
- `web/embed.go` build fail (workaround: `mkdir web/dist`)

---

## Decision: follow-up deferred

Per user decision on 2026-06-15, the 9 informational findings were **left as backlog** rather than opening a follow-up PR. Rationale:

- All findings are quality / coverage, not correctness
- Atomic-PR principle: keeping this PR focused on coverage push
- This file is the source of truth for future cleanup
- Trigger: next time `internal/orchestrator/*_test.go` is touched

---

## Recommended cleanup order (for future devs)

1. **Brittleness** (finding #5) — replace private-field assertions with public API behavior assertions. Highest impact: refactors break fewer tests.
2. **Redundancy** (finding #7) — fold `plugin_adapters_coverage_test.go` content into existing `plugin_adapters_test.go`; drop pure smoke tests.
3. **Naming** (finding #6) — rename `_coverage_test.go` files to `_test.go`; drop `_Cov` suffix from test function names.
4. **Edge cases** (findings #2, #3, #4, #8, #9) — add tests for: `maxAttempts<1` default, `ResolvePrompt` injection + `os.ReadFile` fallback, `loadRecOverrides` expired/empty paths, `CalculateFactorScores` numeric assertions, `WithSetters` behavioral coverage.

Estimated effort: 1 PR, ~3-5 hours.

---

## Files

- `internal/orchestrator/plugin_adapters_coverage_test.go` (241 lines)
- `internal/orchestrator/plugin_registry_coverage_test.go` (247 lines)
- `internal/orchestrator/system_coverage_test.go` (345 lines)

Total: 833 lines, test-only, no production code.
