// performance-report shell — loaded by main.js SHELL_LOADERS
// The page container is pre-rendered in index.html.
// Data loading handled by loadPageData('performance-report') in main.js.

export const template = `
  <div id="pr-root" class="performance-report-root">
    <div id="performanceReportContainer"></div>
  </div>
`;

export async function init() {
  // component-init.js handles performance report rendering
}
