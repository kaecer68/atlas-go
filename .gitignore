.DS_Store
.env
.env.local
/bin/
/dist/
/coverage.out
/atlas
/atlas-go
/atlas-mcp-server-sdk-spike
/agent-guard
/tmp/
/.gocache/
/data/cache/
# data/state/ — ignore runtime data; track _metadata.json + README.md for governance
/data/state/**
!/data/state/
!/data/state/*/
!/data/state/**/_metadata.json
!/data/state/**/README.md
# Data files: generated CSVs/JSONLs are runtime artifacts.
# CSV is the canonical replay source (auto_backfill appends to it daily).
# JSONL is auto-converted from CSV by the auto_backfill background task.
# fundamentals.json is reference/seed data (manually maintained).
/data/replay/*.csv
/data/replay/*.jsonl
# sector_closure_policy.jsonl is a simulation output (writer at
# internal/sectorallocation/policy.go:88); treat as derived runtime data.
/data/sector/
/prompts/experiments/
/internal/experiment/prompts/
/janus-backtest
/janus-status
/validate-narrative-shock
/validate-stress-index
/validate-twse-capital-flow
/internal/monitoring/data/
/prompts/agents/spawn_*.md
/internal/orchestrator/prompts/agents/spawn_*.md
/internal/orchestrator/traces/
# Test/sim runtime session artifacts written under cwd-relative ledger path (test-session positions.json)
/internal/orchestrator/sessions/
cmd/atlas/prompts/agents/spawn_*.md
cmd/experimental/*/prompts/agents/spawn_*.md
/cmd/run-experiment/prompts/
/validate-phase3-integration
/staging-drill
/validate-broker
/test-fugle
/test-hybrid
/test-monitor
*.test
/complexity_top20.txt
cmd/*/reports/
internal/*/data/
!internal/marketdata/data/
!internal/config/data/
cmd/experimental/*/data/
# GitNexus: local index cache. Not tracked — regenerated by `npx gitnexus analyze`.
# See docs/tools.md for indexer setup; CI no longer reads committed metadata.
.gitnexus/
# codebase-memory: local knowledge graph index. Not tracked — regenerated by `codebase-memory_index_repository({mode:"full"})`.
# See docs/tools.md for indexer setup; SQLite-backed at .codebase-memory/ (graph.db + graph.db.zst artifact).
.codebase-memory/
/.worktrees/

# Development artifacts
/.omo/
**/.omo/
/.opencode/*
!/.opencode/commands/
!/.opencode/prompts/
!/.opencode/ocx.jsonc
!/.opencode/package.json
!/.opencode/worktree.jsonc
/.ocx/
/.planning/
/.playwright-mcp/
/.sisyphus/

# Temporary files
/tmp/

# Local environment / secrets
/.fubon-env/

# Build artifacts
/run-stress
/coverage_all.out
/backtest-pipeline

# Compiled binaries (root level)
/atlas-staging
/backfill-replay
/backtest-window
/check-data-health
/daily-replay-sync
/run-experiment
/geo-ingest
/import-replay
/judge-experiment
/macro-ingest
/migrate-data
/promote-baseline
/revert-baseline
/realtime-quote

# Log files
*.log
*.log.*
log/
services/fubon-proxy/log/

# Python cache
__pycache__/
*.pyc
*.pyo

# Experiment snapshots
cmd/run-experiment/data/state/parameter-snapshots/

# Data files (generated)
cmd/backtest-window/data/state/darwinian_weights.json
internal/orchestrator/darwinian_weights.json
data/reports/

# OpenCode config (local development)
opencode.json

# Debug test files
internal/experiment/debug*_test.go

# Test artifacts
/test-results/
# Generated by TestMain_PublicAPI on every run — never commit (golden is the source of truth)
cmd/atlas/testdata/main_api.actual.json

# PR description
/PR_DESCRIPTION.md
# Runtime state and cached data
/channel_health.json
cmd/atlas/data/state/
cmd/atlas/channel_health.json
/staging.pid
internal/marketdata/data/state/
/users.db
/subscriptions.db
# Migration tool binaries
/archive-state
/check-persistence-format
/convert-baseline-policy
/convert-experiment-results
/convert-experiments-jsonl
/convert-recommendation-outcomes
/rewrite-state
# State archives
data/state-archive/

# Legacy runtime reports (now rendered on-demand)
/reports/

# Stale prompt archives from spawning runs
prompts/archive/

# Incomplete Phase C packages
/migrate-jsonl-to-sqlite
# Auto-generated macro snapshot (regenerated by macro-ingest)
/data/state/macro/latest.json
/.gstack/
/calibrate-parameters
/calibrate-seasonal

# LLM phase CLI binaries
/lint-pr
/lint-prompts

# Build artifacts
/fetch-historical

# Coverage reports
*.out

# Node.js
/node_modules/
admin_web/node_modules/
admin_web/dist/
admin_web/test-results/
client_web/node_modules/
client_web/dist/
client_web/test-results/
shared_web/node_modules/
shared_web/dist/

/gentags
/optimize-parameters
/stress-test
/validate-risk-gate
/samples/replay/*.jsonl

# compiled go binaries in repo root
/validate-parameters
.claude/worktrees/
.claude/scheduled_tasks.lock
# Per-user Claude Code settings (e.g. PreToolUse hook registration).
# Convention: settings.json is team-shared; settings.local.json is per-user.
.claude/settings.local.json
configs/*.lock
cmd/cleanup-channel-health/cleanup-channel-health
internal/risk/configs/
# Playwright test results (per-subproject)
admin_web/test-results/
client_web/test-results/
admin_web/web/
client_web/web/
.screenshots/
*.snapshot.bak

# Local editor configs
.vscode/
.idea/

# L2.4 pre-flight validator build artifact (cmd/experimental/l2-4-preflight/main.go)
# Build via: go run ./cmd/experimental/l2-4-preflight
l2-4-preflight

# Local backfill binary build artifact (prevent build-leftover pollution)
/backfill-fundamentals-ps-sector

# Stale esbuild CSS output to shared_web/static/js/ (canonical CSS is in shared_web/static/css/)
/shared_web/static/js/css/
logs/
data/staging/
internal/narrative/testdata/*.actual.json

# Binary freshness protocol (2026-07-21): host-built binaries for sandbox rebuilds
.build-atlas/
.build-cron/

# FinMind daily quota runtime state (written by daily_quota.go during tests/integration)
/cmd/cron-quote-backfill/data/

# iMac production deploy-only compose files (方案二: 部署配置不進 git, 由 iMac 本機維護)
/docker-compose.prod.yml
/docker-compose.crons.yml

# docs/ 禁止的 transient 子目錄（內容一律在 .omo/，防 future drift；2026-08-17 治理）
docs/investigations/
docs/work/
docs/archive/
.prime/

# CodeGraph local index (2026-08-22, iMac)
.codegraph/
